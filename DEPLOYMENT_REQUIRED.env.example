# Copy to the hosting provider's environment settings; do NOT commit real values.
# WAB-TKD uses the current Supabase publishable client key name below.
VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=
# Optional production identifiers:
VITE_TOURNAMENT_ENV=production
