# WAB-TKD — إصلاح استدعاء لاعبي 1v1 بدون إجبار إدخال الأسماء

- استدعاء Player Call في المباراة العادية 1v1 لا يطلب إدخال اسم يدوياً.
- الأولوية دائماً لاسم اللاعب الموجود في MatchState.
- إذا لم توجد بيانات لاعب، يستخدم Blue: `CHUNG (청)` و Red: `HONG (홍)` كـ fallback للأنيميشن فقط.
- لا يتم إنشاء لاعب وهمي في قاعدة البيانات ولا تغيير نتيجة المباراة أو التسجيل.
- تم تطبيق نفس الـfallback داخل reducer حتى لا يستطيع أي مسار استدعاء قديم تعطيل الأنيميشن بسبب اسم مفقود.
