# WAB-TKD — أرشيف حالة الفئات (دوائر مضيئة) — 2026-09-04

## الفكرة المطلوبة
شاشة أرشيف رئيسية واحدة تعرض TOURNAMENT → GENDER → AGE CATEGORY → كل أوزان تلك الفئة دفعة واحدة، كل وزن بدائرة حالة مضيئة (FINISHED / IN PROGRESS / READY / NOT STARTED)، بدل الدخول لكل وزن على حدة لمعرفة حالته.

## لماذا بدون Migration لقاعدة البيانات
فحصت الـ schema الحالي (`supabase/migrations/`): كل تركيبة Gender+AgeGroup+Weight أصلاً هي صف مستقل في جدول `tournaments` (آلية "category tournament bucket" الموجودة في `src/lib/category-library.ts`، دالة `getOrCreateCategoryTournament`) — لكل واحدة id مستقل ومبارياتها الخاصة عبر `matches.tournament_id`. يعني البنية المطلوبة (كل تركيبة مستقلة تمامًا) **موجودة أصلاً**، والناقص فقط هو شاشة تعرض الكل دفعة واحدة بدل الدخول خطوة بخطوة. لذلك ما احتجنا لمس بنية البيانات ولا خطر على بطولاتك المحفوظة.

## الإضافات (كل شيء إضافي، ولا شيء حُذف أو غُيّر في القديم)
- `src/lib/category-library.ts`: دالة جديدة `listCategoryOverview()` — تجيب كل التراكيب (Gender × Age × Weight) المعروفة (Presets + أي وزن مخصص أضفته سابقًا)، وتحسب حالة كل واحدة من مبارياتها بأقل عدد ممكن من الاستعلامات (استعلامان فقط بغض النظر عن عدد الفئات، بدل استعلام لكل فئة).
- `src/components/CategoryStatusArchive.tsx`: شاشة جديدة — MEN / WOMEN → كل Age Group → كل الأوزان كدوائر مضيئة. الضغط على أي دائرة يفتح نفس شاشة "مكتبة الفئات والأوزان" الموجودة عندك (`CategoryMatchBrowser`) لكن مباشرة على تلك الفئة (بدون خطوات Gender/Age/Weight).
- `src/components/CategoryMatchBrowser.tsx`: prop اختياري جديد `initialBucket` يسمح بفتحها مباشرة على فئة معروفة (يستخدمه الأرشيف الجديد فقط، لا يغيّر سلوكها القديم عند فتحها من الزر الأصلي).
- `src/components/TournamentManager.tsx`: زر جديد "أرشيف حالة الفئات" بجانب زر "مكتبة الفئات والأوزون" الموجود — يفتح الشاشة الجديدة فوق الحالية، لا يستبدلها.
- `src/index.css`: أنيميشن نبض جديد للفئات الجارية (Glow → Pulse → Glow → Pulse) مطابق لما طلبته.

## منطق الحالة (مُشتق من المباريات، غير مخزّن بشكل منفصل)
- ⚪ NOT STARTED: لا توجد مباريات بعد لهذه الفئة.
- 🟡 READY: مباريات موجودة لكن لم تبدأ أي منها (كلها `waiting`).
- 🔵 IN PROGRESS (نابض): مباراة حيّة (`fighting`/`rest`) الآن، أو بعض المباريات انتهت وبعضها لا.
- 🟢 FINISHED: كل مباريات الفئة انتهت.

## نقطة يجب معرفتها
- **لا يوجد اكتشاف تلقائي لحالة 🔴 "مشكلة/توقيف"** — لا يوجد في نموذج البيانات الحالي أي علامة تشير إلى أن فئة معينة متوقفة، فما أضفتها تخمينًا. إذا تحتاجها فعليًا، تحتاج نقطة بيانات جديدة (مثلاً حقل `flag` على المباراة أو الفئة) — أخبرني إذا تريدها وأنفذها بشكل صريح بدل التخمين.
- سقف وزن الفريق في Par Équipe (300/260/160+135 كغم) اللي كنا ناقشينه **لم يُنفَّذ بعد** — بانتظار تأكيدك: تريدها أرقام ثابتة أم قابلة للتعديل من إعدادات البطولة نفسها؟
