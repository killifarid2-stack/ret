# WAB-TKD — Animation & Call Flow Fixes

## تم إصلاحه

1. **Intro Video**
   - لم يعد الـSplash ينهي العرض قبل وصول الفيديو إلى `ended`.
   - الـfade الأخير أصبح بعد انتهاء الفيديو، وليس قبله.
   - زر/اختصار Skip ما زال متاحًا.
   - تم منع إعادة تشغيل الفيديو بسبب تغيّر callback أثناء إعادة الرسم.
   - تم تعديل fallback الطويل حتى لا يقطع فيديو طبيعيًا طويلًا.

2. **استرجاع شاشة NEXT ON THE MATCH**
   - تمت إضافة `NextOnMatchOverlay.tsx`.
   - تظهر شاشة الفريقين/الناديين والشعارات واللاعب التالي عندما تكون شاشة الاستدعاء في حالة انتظار.
   - لا تغطي أزرار الحكم لأنها شاشة الجمهور فقط.

3. **استرجاع Team Call على شاشة الجمهور**
   - `ExactTeamCallBroadcast` أصبح يُعرض فعليًا عندما تكون `callAnimation.phase === 'team'`.
   - كان المكوّن موجودًا لكن غير مركّب داخل العرض العام.

4. **AUTO / MANUAL**
   - أضيف مفتاح واضح داخل إعدادات Animation Settings:
     - `AUTO / تلقائي`
     - `MANUAL / يدوي`
   - AUTO: BLUE → انتظار تأكيد الحكم → RED → انتظار تأكيد الحكم → MATCHUP.
   - MANUAL: الحكم يختار الفريق/اللاعب يدويًا.

5. **تأكيد وصول اللاعب وإعادة النداء**
   - زر `CONFIRM ARRIVAL` لتأكيد وصول اللاعب.
   - زر `RECALL PLAYER` لإعادة النداء.
   - زر `RECALL TEAM` لإعادة نداء الفريق.
   - التدفق التلقائي لا يتجاوز تأكيد الحكم.

6. **معاينة شاشة الجمهور**
   - تمت إضافة `AUDIENCE LIVE PREVIEW` داخل إعدادات الأنيميشن.
   - المعاينة لا تغطي لوحة الأزرار.

## التحقق
- `tsc --noEmit` نجح بدون أخطاء.
- تعذر تشغيل `npm run build` لأن تثبيت dependencies داخل البيئة انتهى بمهلة ولم يكتمل، وليس بسبب خطأ TypeScript.
