# Release Scope

This package is a release candidate. Actual Production deployment, database credentials, and physical MAT registration require the operator's real environment and credentials.

The project intentionally contains no real secrets. Use the environment configuration provided by the deployment platform.
