# WAB-TKD — Main Referee Full Screen

- Added `VIEW ALL / FULL SCREEN` to the Main Referee header.
- Full-screen mode uses the complete application viewport (`100vw × 100vh`) and removes the previous in-app side black margins.
- `ESC` exits full-screen mode.
- Added stronger tournament/operator lighting and a focused gold command treatment without changing scoring or match data.
- Existing Player Call, Team Call, scoring, save, replay and judge controls remain in place and isolated.
- No database, scoring engine, tournament engine, player database or save/restore schema was changed.
