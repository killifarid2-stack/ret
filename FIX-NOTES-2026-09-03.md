# WAB-TKD — Runtime / Broadcast Fixes — 2026-09-03

## Fixed
- Removed the runtime dependency on `Maximize2` / `Minimize2` in the Main Referee, Player Call controls, and Tournament File Center UI; these controls now use the stable `Maximize` / `Minimize` icons.
- Public-display standby no longer hides an explicitly triggered Player Call, Matchup, Team Call, substitution cinematic, or finished-match Winner cinematic. These referee-triggered broadcast layers are allowed to render even when the general Public Broadcast LIVE flag is off.
- Existing Individual Winner Animation routing remains individual-only; Par Équipe keeps its team winner path.
- Existing Player Call renderers/assets remain intact and isolated between Individual and Par Équipe.

## Preserved
- Tournament and Par Équipe local save/archive flows.
- Match/scoring/timer state.
- Existing Player Call / Team Call animation assets and data binding.
- Main Referee as the controller; Public Display remains display-only.
