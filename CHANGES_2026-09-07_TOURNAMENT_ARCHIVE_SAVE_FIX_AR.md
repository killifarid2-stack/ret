# إصلاح شامل لحفظ البطولة والأوزان واللاعبين — 2026-09-07

## المشكلة
- مركز الملفات كان يعرض عدد اللاعبين = 0 لبعض البطولات رغم أن قائمة اللاعبين محفوظة داخل النسخة المحلية للبطولة.
- نسخة السحابة من `tournaments` لا تحتوي roster اللاعبين داخل صف البطولة؛ اللاعبين محفوظون في جدول `players`، وكان loader يستبدل النسخة المحلية الغنية بنسخة السحابة الفقيرة.
- إحصائيات الأندية كانت تحسب فقط الأندية التي ظهرت في مباريات مكتملة، وليس الأندية المسجلة في الوزن.
- حالة الوزن كانت قد تبقى غير صحيحة لأن نسخة السحابة ونسخة local match cache كانتا تُحسبان مرتين لنفس المباراة.
- مفتاح حفظ المباراة المحلي كان يعتمد على `competition_name + match_number`، ما يسمح بتصادم Match #1 بين وزنين مختلفين داخل نفس البطولة.
- أوزان بدأت بالفعل كانت تعرض أحياناً كـ VIEW ONLY بدل زر متابعة واضح.
- مباريات League المنتهية كانت تحتفظ بزر Start، ما يسمح بمحاولة إعادة مباراة انتهت.

## ما تم إصلاحه

### 1. حفظ واسترجاع اللاعبين
- تحميل roster اللاعبين من جدول `players` لكل بطولة سحابية.
- دمج cloud + local field-by-field بدل إسقاط `players` و`display_colors` ونسخة bracket المحلية.
- عند فتح بطولة محلية يتم تفريغ roster القديم أولاً ثم تحميل roster البطولة المطلوبة، لمنع تسرب لاعبي وزن سابق إلى وزن آخر.

### 2. مركز الملفات
- عدد اللاعبين أصبح يعتمد على roster المسجل + لاعبي bracket كـ fallback.
- عدد الأندية يشمل الأندية المسجلة قبل بدء أول مباراة.
- الأوزان التي بدأت تعرض `CONTINUE WEIGHT`.
- الوزن المنتهي يعرض `FINISHED · OLD MATCHES LOCKED`.
- عرض عدد الأندية داخل إحصائيات الوزن.

### 3. حالة الوزن والألوان
- أرشيف الفئات يجمع بيانات roster المحلي والسحابي.
- يتم جلب لاعبي السحابة من `players` حتى تظهر الأعداد الصحيحة.
- يتم تجنب عدّ نفس المباراة مرتين عندما توجد نسخة cloud ونسخة local.
- بعد حفظ المباراة، يبقى bracket والحالة مرتبطين بالبطولة الصحيحة، فتتغير الحالة من READY إلى LIVE/IN PROGRESS ثم FINISHED حسب المباريات الفعلية.

### 4. حفظ المباريات بين الأوزان
- تم تغيير مفتاح local match ليشمل `tournament_id` و`match_id`.
- هذا يمنع Match #1 في وزن من الكتابة فوق Match #1 في وزن آخر داخل نفس البطولة.
- `loadMatchesLocal` يدعم مفاتيح النظام القديم ويزيل التكرار عند وجود النسخة القديمة والجديدة.

### 5. ترقية البطولة من Local إلى Cloud
- عند نجاح رفع بطولة محلية إلى Supabase، يتم نقل match records المحلية من الـ local tournament id إلى الـ cloud tournament id قبل حذف الغلاف المحلي.
- نفس الحماية مطبقة في زر `SYNC TO CLOUD`.
- هذا يمنع أن تصبح البطولة بعد المزامنة ظاهرياً بلا مباريات مكتملة.

### 6. المباريات التالية
- مباريات League المكتملة أصبحت مقفلة ولا يظهر لها Start.
- المباراة التالية غير المكتملة تعرض `START NEXT` عندما تكون هي أول مباراة قابلة للعب.
- في نظام Bracket، `MatchStrip` يبقي المباراة المنتهية غير قابلة لإعادة التشغيل، ويظل مسار الفائز هو الذي يحدد المباراة التالية.

## ملاحظة الاختبار
تم فحص التعديلات بنيوياً وجرى تشغيل TypeScript parser على الملفات المعدلة. تعذر إجراء `npm run build` الكامل لأن تثبيت `node_modules` في بيئة التنفيذ انقطع بسبب timeout؛ لذلك لا يتم الادعاء بأن build الإنتاج الكامل تم تشغيله هنا.
