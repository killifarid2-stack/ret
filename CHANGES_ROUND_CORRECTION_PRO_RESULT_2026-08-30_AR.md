# WAB-TKD — Round Correction / PTG / Golden Point / Main Referee Result Update

## 1. Round Correction
- تصحيح الجولة لم يعد يقفز مباشرة إلى نتيجة المباراة.
- بعد `SHOW UPDATED RESULT` تعود الحالة إلى `REST` مع `00:00` أولًا.
- شاشة الجمهور تعود إلى إطار LIVE MATCH / REST.
- الحكم يرى زر `START BREAK AGAIN` لإعادة تشغيل وقت الراحة الرسمي.
- إذا أصبحت المباراة محسومة بسبب التصحيح، لا تظهر النتيجة الرسمية إلا بعد انتهاء وقت الراحة المعاد تشغيله.
- إذا لم تصبح المباراة محسومة، ينتهي وقت الراحة وتعود المباراة إلى حالة انتظار بدء الجولة التالية.

## 2. PTG
- تمت إضافة `SKIP PTG` واضح ومرئي بجانب تأكيد PTG.
- Skip PTG لا يغيّر النقاط أو نتيجة الجولة؛ فقط يزيل قفل PTG ويعيد استمرار الجولة.

## 3. Golden Point
- زر `Golden Point Animation` يظهر فقط عندما تكون الجولة الحالية فعلًا `isGoldenRound` وفي حالة انتظار بدء الجولة.
- لا يظهر زر الأنيميشن في الراحة العادية أو الجولات العادية.

## 4. Main Referee Result
- أزيلت شاشة الفائز ذات الخلفية الزرقاء/الحمراء التي كانت تغطي واجهة الحكم بالكامل.
- يعتمد الحكم الآن على لوحة `OFFICIAL MATCH RESULT` الاحترافية داخل الواجهة، مع النتيجة والفائز والإحصاءات وأزرار الحفظ/السجل/PDF/العودة.

## 5. Validation
- تم فحص ملفات TypeScript/TSX المعدلة باستخدام TypeScript transpile parser، ولا توجد أخطاء syntax في الملفات المعدلة.
- لم يتم تشغيل production build لأن dependencies غير مثبتة في بيئة التنفيذ؛ `npm install` تجاوز مهلة التنفيذ.
