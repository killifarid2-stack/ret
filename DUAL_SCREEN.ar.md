# شاشة الجمهور المزدوجة (Electron)

هذا يضيف نظام شاشتين احترافي فوق تطبيق Electron الحالي:

- **شاشة المشغّل (Operator)** — النافذة الأساسية للتطبيق (بدون تغيير)، حيث تُدار
  `/operator`، `/admin`، إلخ.
- **شاشة الجمهور (Public Scoreboard)** — نافذة ثانية بلا حدود وبملء الشاشة تعرض
  دائمًا `/scoreboard`، مخصّصة للعرض على تلفاز عبر HDMI أو شاشة لاسلكية متصلة
  بجهاز البطولة.

## الملفات

| الملف | الوظيفة |
|---|---|
| `electron/main.cjs` | أُعيدت كتابته. يدير النافذتين، يكتشف اتصال/فصل الشاشات، يفتح/يغلق/ينقل شاشة الجمهور، يحفظ الشاشة المختارة، وينقل رسائل مزامنة حالة المباراة عبر IPC. |
| `electron/preload.cjs` | **جديد.** يوفّر جسرًا آمنًا `window.electronAPI` (متوافق مع contextIsolation، دون تسريب صلاحيات Node إلى الصفحة). |
| `src/types/electron.d.ts` | **جديد.** أنواع TypeScript لـ `window.electronAPI`. |
| `src/context/MatchContext.tsx` | نافذة المشغّل الآن تبث كل تغيير في حالة المباراة عبر IPC؛ نافذة الجمهور لا تُشغّل ساعتها الخاصة وتكتفي بعرض ما يصلها، فلا يمكن أن تتفاوت الشاشتان أبدًا. |
| `src/App.tsx` | أُضيف `PublicWindowGate` الذي يوجّه النافذة الثانية مباشرة إلى `/scoreboard` عند التحميل. |
| `src/components/PublicDisplayControl.tsx` | **جديد.** أزرار "فتح/إغلاق شاشة الجمهور" + قائمة اختيار الشاشة، تظهر في الشريط العلوي (فقط داخل Electron — لا تظهر في نسخة الويب/PWA). |
| `src/components/TopNav.tsx` | يعرض الآن `PublicDisplayControl`. |
| `src/lib/i18n.ts` | أُضيفت نصوص عربية/إنجليزية للعنصر الجديد. |

## آلية العمل

1. عند تشغيل التطبيق، يبحث `main.cjs` عن أي شاشة غير الشاشة الرئيسية.
   إن وُجدت، تُفتح شاشة الجمهور تلقائيًا عليها، بملء الشاشة وبدون إطار.
2. `screen.on('display-added' / 'display-removed')` يراقب أحداث توصيل/فصل
   HDMI أو الشاشة اللاسلكية طوال الجلسة:
   - عند التوصيل ← تُفتح شاشة الجمهور تلقائيًا (إلا إذا أغلقها المشغّل يدويًا
     سابقًا بينما كانت الشاشة لا تزال متصلة).
   - عند الفصل ← تُغلق شاشة الجمهور تلقائيًا، ويُعاد ضبط علامة "أُغلقت يدويًا"
     بحيث تُفتح تلقائيًا مجددًا عند إعادة التوصيل التالية.
3. لا يمكن أن توجد أكثر من نافذة جمهور واحدة أبدًا — عند استدعاء
   `openPublicWindow()` مع وجود نافذة مفتوحة، يتم التركيز عليها بدل إنشاء نافذة
   ثانية.
4. مُعرّف الشاشة المختارة يُحفظ في
   `app.getPath('userData')/display-config.json`، فيُتذكّر عند إعادة تشغيل
   التطبيق وعبر عمليات إعادة التوصيل.
5. كل تغيير في حالة المباراة (الوقت، النقاط، الجولات، العقوبات (غامجيوم)،
   أسماء اللاعبين، الأعلام/الجنسية — أي المباراة بأكملها) يُبَث فورًا من نافذة
   المشغّل إلى نافذة الجمهور عبر
   `ipcRenderer.send('match-state-broadcast', state)` ←
   تنقله `ipcMain` ← `publicWin.webContents.send('match-state-sync', state)`.
6. أزرار "فتح شاشة الجمهور" / "إغلاق شاشة الجمهور" وقائمة اختيار الشاشة موجودة
   في الشريط العلوي (تظهر فقط عند وجود `window.electronAPI`، أي داخل تطبيق
   Electron).

## طريقة التشغيل

```bash
npm install
npm run build          # يبني تطبيق الويب داخل dist/
npm run electron:build # يجهّز تطبيق Electron (أو npx electron . بعد البناء لتجربة سريعة)
```

## ملاحظة عن نقص موجود مسبقًا (لا علاقة له بهذه الميزة)

يستورد كل من `src/App.tsx` و`src/hooks/use-toast.ts` من `@/components/ui/*`
(مكونات toaster/sonner من shadcn)، لكن هذا المجلد **غير موجود** في هذه النسخة
المُصدَّرة من المشروع. هذا سيؤدي إلى فشل `npm run build` برسالة خطأ
"Cannot find module '@/components/ui/toaster'" — وهي مشكلة لا علاقة لها بميزة
الشاشتين المذكورة أعلاه. يمكن توليد هذه المكونات عبر
`npx shadcn@latest add toast sonner`، أو اطلب مني تجهيز بدائل بسيطة لها.

## v48 — الاختيار الديناميكي للتلفاز

الشاشة الخارجية لا تفتح تلقائيًا. عند اكتشاف HDMI أو شاشة لاسلكية جديدة تظهر نافذة للمشغل لاختيار: المباراة الحية، إعلان البساط، أو المباريات القادمة. لكل تلفاز وضع وبساط مستقل، ويمكن تغيير القرار لاحقًا دون فصل التلفاز.
