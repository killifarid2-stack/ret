# التعديلات المضافة

## 28. إصلاح كراش "Cannot access 'r' before initialization"
- **السبب**: `vite.config.ts` كان كيقسم الكود يدويا لعدة "vendor chunks" ثابتة
  (`vendor-react`, `vendor-radix`, `vendor-supabase`, `vendor-charts`) عبر
  `manualChunks`. هاد التقسيم كيخلق "chunks" (ملفات JS نهائية) كتعتمد على
  بعضها بشكل دائري (مثلا: chunk `OperatorScreen` [محمل بـ`lazy()`] كيعتمد
  على `vendor-radix` اللي هو بدورو كيعتمد على `vendor-react`، واللي
  `OperatorScreen` كيعتمد عليه مباشرة زيادة) — دورة على مستوى الملفات
  المبنية (bundled chunks)، حتى إلا ماكانتش عندنا استيراد دائري فالكود
  المصدري (تأكدت بـ`madge` أنه ماكاينش).
  فالمتصفح العادي هاد الشي كيتخبى لأن كل الملفات كيتحملو بالتوازي (HTTP/2)
  ومحلل ES modules كيرتب التنفيذ صحيح بغض النظر عن ترتيب الوصول. لكن
  التطبيق المكتبي (Electron) كيحمل `index.html` عبر `file://`
  (`electron/main.cjs` → `win.loadFile`)، فين التحميل كيكون أكثر حساسية
  للترتيب — فحالات معينة كيتنفذ كود chunk قبل ما يكمل chunk آخر معتمد
  عليه التهيئة ديالو، وهادشي بالضبط كيطلع خطأ
  `Cannot access 'X' before initialization` (خطأ TDZ) اللي كيلقطو
  `ErrorBoundary` ويبين شاشة "حدث خطأ غير متوقع".
- **الحل**: حيت `manualChunks` من `vite.config.ts` وخليت Rollup يحسب
  تقسيم الملفات أوتوماتيكيا من شجرة الاعتماديات الحقيقية، اللي كتفادى هاد
  النوع من الدورة تماما.
- **تحقق**: `npm run build` نجح بلا أي خطأ، و`npx vitest run` (11 اختبار)
  نجحو كاملين.

## نقطتين تأكدت أنهم خدامين ديجا فهاد النسخة (بلا تعديل إضافي):
- **كسر التعادل بالإنذارات أولا**: `src/lib/ai-tiebreaker.ts` و
  `src/lib/match-engine.ts` (`endRound`) كيتحققو من عدد الگامجيوم قبل كل
  حاجة أخرى — اللي عندو إنذارات أقل كيربح مباشرة، وغير كي يكونو متساويين
  كيرجع لجودة الضربات (تفاصيل فبند 23 تحت).
- **أنيميشن الفائز للمباراة الفردية (لاعب ضد لاعب)**: `PublicScoreboard.tsx`
  (قسم `match_end`) عندو ديجا ستايل "MATCH X RESULT" + بانر "WINNER" ذهبي
  + علم/صورة اللاعب مؤطرة جنب بانر الاسم بلون الفريق — بحال الصورة
  المرجعية. هاد الستايل خاص بالمباراة الفردية فقط (`!isTeamReveal`)، بار
  إيكيب عندها ستايل شعار/درع مختلف كيفما تفصل فبند 25-26.

## 27. أنيميشن تبديل اللاعب المميز + لوحة ترتيب الدوري فشاشة الجمهور
- **أنيميشن تبديل اللاعب (بار إيكيب)**: قبل كان كيستعمل نفس كارد النداء
  العادي بغير badge صغيرة "Player Change". دابا عندو تصميم خاص بيه:
  اللاعب الخارج يبان صغير وباهت (رمادي، خط فوق الاسم) على اليسار، سهم
  نابض فالوسط (⟶)، واللاعب الداخل كبير وبارز على اليمين — تركيبة "X ⟶ Y"
  كتوضح لحكام والجمهور أن هادي عملية تبديل، ماشي استدعاء جديد من الصفر.
  الملفات: `src/types/tkd.ts` (`outgoingName/outgoingPhoto/outgoingNationality`
  فـ`CallAnimation`)، `src/context/MatchContext.tsx` (`CONFIRM_SUBSTITUTION`
  كيلقط بيانات اللاعب الخارج قبل ما يبدلها)، `src/components/PublicScoreboard.tsx`.
- **لوحة ترتيب الدوري (Round-Robin)**: زر جديد "الترتيب" جنب شريط المباريات
  فشاشة المشغل (كي تكون البطولة نوعها دوري) كيبدل/يخبي جدول ترتيب كامل
  فشاشة الجمهور: الرتبة، الاسم+العلم+النادي، لعب/فاز/خسر، النقط له/عليه،
  الفارق — الصف الأول مميز بالذهبي. الترتيب كيتحسب أوتوماتيكيا من نتائج
  الدوري المحفوظة (`computeLeagueStandingsDisplay`) ويتبعث لشاشة الجمهور
  عبر الحالة (`state.leagueStandings` / `state.showStandings`).
  الملفات: `src/lib/league-standings.ts` (جديد)، `src/types/tkd.ts`
  (`LeagueStandingEntry`)، `src/context/MatchContext.tsx`
  (`SET_LEAGUE_STANDINGS`, `SET_SHOW_STANDINGS`)، `src/components/OperatorScreen.tsx`،
  `src/components/PublicScoreboard.tsx`.
- **تحقق**: `tsc --noEmit` (بلا أي خطأ جديد — كل الأخطاء المتبقية تأكدت
  أنها موجودة من قبل فنفس المواقع بالضبط، من خدمة سيسيونات سابقة) و
  `npm run build` نجح.
- **الحالة العامة**: باقي من الأفكار المطلوبة: إكمال الترجمة
  (`OperatorScreen.tsx` ~55 نص، `TournamentManager.tsx` ~28، `Index.tsx` ~19)
  وتقرير PDF شامل للبطولة — غادي نكملهم فرسائل جاية.

## 26. أنيميشن الفائز الفردي: ستايل بث تلفزي (علم/صورة مؤطرة + بانر اسم)
بناء على مثال صورة حقيقية بعثيتيها — بدّلت ستايل "السبوت لايت" الدائري
اللي درت قبل (كان معقد بزاف/بعيد عن المرجع) بستايل أقرب للبث التلفزي
الكلاسيكي المطلوب:
- **مباراة فردية**: علم مستطيل كبير (بحال المرجع) بإطار أبيض نظيف وظل، جنب
  بانر الاسم بلون الفريق — بالضبط بحال الصورة المرفقة.
- **إلا كانت صورة اللاعب موجودة**: كتبان الصورة **بإطار** (بدل العلم) — نفس
  الإطار الأبيض والظل، بحال ما طلبتي.
- بار إيكيب بقات بستايل "الدرع/الشوكويف" اللي درنا قبل (ماشي محتاج تغيير).
- الملف: `src/components/PublicScoreboard.tsx` (قسم `match_end`).
- **تحقق**: `tsc --noEmit` و`npm run build` نجحو بلا أي خطأ جديد.

## 25. أنيميشن الفائز: قوة وتمييز + فرق بين بار إيكيب ومباراة فردية
قبل، الكارد المركزي (الصورة/الشعار جنب الاسم) كان بنفس الحجم والستايل فالحالتين
(علم صغير 80px للفردي، شعار 84px للفريق) — بلا أي تمييز بصري حقيقي. دابا:

- **مباراة فردية (لاعب ضد لاعب)** — ستايل "بروجيكتور/سبوت لايت": صورة اللاعب
  (أو علمو إلا ماكانتش عندو صورة) كبيرة (108px)، دائرية، بأشعة ضوء دايرة
  كتدور وراها (`gpRayRotate`) وپالس متوهج (`gpMedalGlow`) — إحساس شخصي،
  البطل تحت الضوء.
- **بار إيكيب** — ستايل "الدرع/الكأس": شعار الفريق (104px) بحلقتين صدمة
  (`gpShockwave`) كيتوسعو وكيختفيو تكرارا، بحال إعلان درع بطولة — إحساس
  جماعي، الفريق كامل.
- الجوج عندهم دخول `gpZoomIn` قوي (zoom + دوران خفيف) بدل الظهور العادي.
- الملف: `src/components/PublicScoreboard.tsx` (قسم `match_end`، الكارد
  المركزي). استعملت أنيميشنات موجودة ديجا فالمشروع (`gpRayRotate`،
  `gpShockwave`، `gpMedalGlow`، `gpZoomIn`، `callGlowPulse`) بلا ما نزيد
  كيفريمز جديدة.
- **تحقق**: `tsc --noEmit` (بلا أي خطأ جديد) و`npm run build` نجح.

## 24. الجواب: واش معلومات مباريات/بطولات بار إيكيب كتتحفظ؟
سؤال مباشر — هاذي خلاصة التحقق + الإصلاحات اللي كانت ناقصة:

- ✅ **مباراة منتهية** (فردية، بار إيكيب): اسم الفريق، شعار الفريق، شعار
  النادي، دولة الفريق، وروستر كامل (كل لاعب) كيتسجلو فجدول `matches` —
  مؤكد من قبل (بند -3، 15-17).
- ✅ **بطولة بار إيكيب** (Tournament Manager، كأس أو دوري): روستر كل فريق
  (`teamRosters`) وطريقة التسيير (`teamPlayMode`) كيتسجلو مع البطولة، وشعار
  الفريق (`team_logo`) كيتسجل مع كل فريق فجدول `players` — كانو خدامين ديجا.
- ❌ **كانو ناقصين، صلحتهم دابا**:
  - **شعار النادي** (الشارة الثانوية) ماكانش لاصلا عندو مكان يتحفظ فيه على
    مستوى الفريق/البطولة (غير على مستوى مباراة حية وحدة فادمين) — زدت عمود
    `club_logo` جديد لجدول `players` (migration جديدة) + واجهة رفع فمدير
    البطولة (جنب شعار الفريق) + كتابة/قراءة كاملة.
  - **دولة الفريق** (العلم) وحتى **اسم النادي كنص** (البادج تحت اسم الفريق)
    ماكانوش كيتنقلو للمباراة كي تبدا من داخل بطولة (كانو خدامين غير كي تبدا
    المباراة من ادمين مباشرة) — دابا `handleStartMatch` كيمرر `teamCountry`
    و`club` (اسم النادي) زيادة على `teamNames`/`teamLogos`/`clubLogos`.
- الملفات: migration جديدة `20260810120000_add_club_logo_to_players.sql`،
  `src/types/tkd.ts`، `src/components/TournamentManager.tsx`.
- **تحقق**: `tsc --noEmit` (بلا أي خطأ جديد — الأخطاء المتبقية موجودة من
  قبل فنفس الملف، غير مرتبطة بهاد التعديل) و`npm run build` نجح.

## 23. كسر التعادل بالذكاء الاصطناعي: الإنذارات (غامجيوم) أولا
- قبل، منطق كسر التعادل (`ai-tiebreaker.ts`) كان كيدمج جودة الضربات + عدد
  الإنذارات فمعادلة واحدة (`advantage`) — يعني ممكن لاعب بإنذارات أكثر
  يربح إلا كانت جودة ضرباتو أحسن بزاف.
- دابا: **عدد الإنذارات (Gamjeom) كيتفقد أولا، قبل كل حاجة أخرى**. إلا
  كان عدد الإنذارات مختلف بين اللاعبين، اللي عندو إنذارات أقل كيربح
  مباشرة — بلا ما ننظرو حتى لجودة الضربات. غير إلا كان عدد الإنذارات
  **متساوي بجوج**، كيرجع للمنطق القديم (جودة الضربات، الضربات عالية
  القيمة، آخر نقطة مسجلة).
- الملف: `src/lib/ai-tiebreaker.ts`.
- **تحقق**: `tsc --noEmit` و`npm run build` نجحو بلا أي خطأ جديد.

## 22. الترجمة الكاملة لـ AdminPanel.tsx (بداية تدقيق i18n الشامل)
- زدت **~105 مفتاح ترجمة** جديد لـ`i18n.ts` (بالعربية والإنجليزية سوا، تحققت
  أن العدد متطابق بين اللغتين: 251 = 251 قبل الإضافة، ونفس التطابق بعدها).
- ربطت `useI18n` بـ`AdminPanel.tsx` (ماكانش مستورد خالص قبل) وبكل الأجزاء
  المساعدة (`InputField`, `SelectField`, `PhotoUploadField`, `RosterEditor`)
  عبر props اختيارية (`selectLabel`, `changeLabel`, `namePlaceholder`...).
- بدّلت كل النصوص المكتوبة مباشرة (معلومات المنافسة، اللاعبون، العرض العام،
  شاشة الاستدعاء [كانت بالعربية الثابتة أصلا — دابا كتترجم مع تبديل اللغة]،
  قواعد المنافسة، النقطة الذهبية، الموافقة التلقائية، بار إيكيب كاملة، سجل
  IVR، زر الحفظ ورسالة الفشل) بـ`t('...')`.
- **تحقق**: `tsc --noEmit` و`npm run build` نجحو بلا أي خطأ جديد.
- **الحالة**: `AdminPanel.tsx` مترجم بالكامل دابا. الباقي: `OperatorScreen.tsx`
  (~55 نص)، `TournamentManager.tsx` (~28 نص)، `Index.tsx` (~19 نص) — غادي
  نكملهم فرسائل جاية.

## 21. شاشة "NEXT ON THE MATCH": الصورة الكبيرة تكون الفريق ماشي اللاعب الفردي
- فبار إيكيب، الصورة الكبيرة/البارزة فوق الكارد (اللي كانت ديما صورة اللاعب
  الفردي الحالي، مثلا "ali") دابا هي **شعار الفريق** (+ شعار النادي كـbadge
  صغير متراكب فوقو، بنفس تركيبة أنيميشن نداء الفريق) — بحال بحال ما هو الحال
  فالأنيميشن، هاد الكارد الثابت دابا كيبين "الفريق" أولا، ماشي اللاعب الفردي.
  - إلا ماكانش عندو شعار فريق ولا شعار نادي (أو ماشي بار إيكيب)، كيرجع
    للسلوك القديم: صورة اللاعب الفردي هي الكبيرة (بحال قبل).
  - حيت الصورة الكبيرة دابا هي الفريق، حذفت الشارتين الصغار المكررين (شعار
    الفريق + شعار النادي) اللي كانو فوق "TEAM A/TEAM B" — بقات غير مرة
    وحدة (الكبيرة).
- الملف: `src/components/PublicScoreboard.tsx` (`PlayerCard`).
- **تحقق**: `tsc --noEmit` و`npm run build` نجحو بلا أي خطأ جديد.

## 20. شاشة الفائز (بار إيكيب): إطارات فردية لكل لاعب + روستر الفريق الخاسر
- بدّلت اللائحة المسطحة (اسماء كـ"badges" نصية) بإطارات فردية لكل لاعب —
  صورة/حروف أول الاسم فمربع بحدود بلون الفريق + علم صغير فالزاوية + الاسم
  تحت — نفس ستايل الإطارات اللي فمرحلة نداء الفريق.
- زدت روستر **الفريق الخاسر** كامل (بنفس ستايل الإطارات) جنب روستر الفريق
  الفائز، فـ2 عمود (خاسر يسار، فائز يمين) كل واحد بعنوانو (اسم الفريق) —
  قبل كان يبان غير روستر الفريق الفائز.
- الملف: `src/components/PublicScoreboard.tsx` (قسم `match_end`).
- **تحقق**: `tsc --noEmit` و`npm run build` نجحو بلا أي خطأ جديد.

## 19. شريط الحكم + انتقال تلقائي بين المباريات فالدوري (Round-Robin League)
- **المشكل**: `MatchStrip` (الشريط اللي كيبان للحكم الرئيسي فوق شاشة
  Operator، فيه كل المباريات مرتبة و"X/Y matches played") كان مبني غير
  لمباريات الكأس (`bracket`، عندها حقل `round` إجباري) — البطولات بنظام
  **الدوري** (`bracket_data.mode === 'league'`) كانت خزنة تحت `bracket_data.league`
  بشكل مختلف، فالشريط ماكانش كيبان خالص فهاد الحالة.
  - `round` بقا اختياري فـ `MatchStripMatch` (`src/components/MatchStrip.tsx`)،
    وما كيبانش "· R{round}" إلا كان موجود.
  - الـ effect اللي كيحمّل بيانات البطولة للحكم (`OperatorScreen.tsx`) دابا
    كيفرّق: إلا كانت `mode === 'league'`، كيحول لائحة الدوري لنفس الشكل
    (`id, player1, player2, winner`) بلا `round`، فيبان الشريط عادي.
- **الانتقال التلقائي بين المباريات**: قبل، كي تسالا مباراة جاية من بطولة،
  الأوبيراتور كان كيرجع لصفحة "Tournament" وخاصو يختار المباراة الجاية
  بيدو. دابا:
  - زدت `pendingAutoStart` + `useEffect` جديد فـ `TournamentManager.tsx`:
    كي يرجع الأوبيراتور بعلامة `autoStartNext` (مبعوتة من `OperatorScreen.tsx`
    مع `continueTournamentId`)، كيلقى أول مباراة "جاهزة" ماشي مسالة (فالدوري:
    أول مباراة بلا `winner`؛ فالكأس: أول مباراة بلا `winner` وعندها الجوج
    لاعبين) وكيبداها مباشرة بنفس المنطق ديال زر "Start" اليدوي — بلا ما
    الأوبيراتور يدير حتى حاجة.
  - هاذشي كيخدم للدوري والكأس بجوج، وبار إيكيب ولا لا.
- **تحقق**: `tsc --noEmit` و`npm run build` نجحو بلا أي خطأ جديد (كاينين
  أخطاء قديمة فملفات أخرى ماشي مرتبطة بهاد التعديل — من خدمة سيسيون سابقة).
- الملفات: `src/components/MatchStrip.tsx`، `src/components/OperatorScreen.tsx`،
  `src/components/TournamentManager.tsx`.

## -3. المشكل: تعديل مباراة جارية (روستر/شعارات) فادمين، Save & Apply مايبانش
- **السبب الجذري**: `handleSave` فـ `AdminPanel.tsx` كان ديما كيبدا بـ
  `dispatch({ type: 'INIT_MATCH', config })` — وهاد الفعل كيبدل **الحالة
  كاملة** بمباراة جديدة فارغة (`createInitialMatchState`)، يعني كيمسح
  النقط/الجولة/التايمر لمباراة جارية. التعديلات (روستر، شعارات...) كانت
  كتنضاف من بعد فوق هاد الحالة الجديدة الفارغة، فكان الأوبيراتور كيرجع لقى
  المباراة "بداية من جديد" — واللي بان لينا كـ"ماتبدلش والو".
- **الحل**: دابا `handleSave` كيتأكد أولا: إلا كانت المباراة **جارية دبا**
  (`state.status !== 'waiting'`، يعني `fighting`/`paused`/`rest`/...) كيدير
  `UPDATE_CONFIG` (تحديث بسيط للقواعد بلا ما يمسح شي حاجة) بدل `INIT_MATCH`،
  ومن بعد كيطبق تعديلات الروستر/الشعارات/الأسماء عادي فوق نفس المباراة
  الجارية بلا ما تنمسح النقط. `INIT_MATCH` بقات مستعملة غير كي تكون
  المباراة مازال ماشي بداية (`waiting`) — أي مباراة جديدة.
- الملف: `src/components/AdminPanel.tsx`.

## -2. إصلاحات بعد ملاحظات مع صور (بار إيكيب)
- **مشكل: الأنيميشن ما كيبداش حتى نضغط "إلغاء الاستدعاء"** — السبب: شاشة
  "NEXT ON THE MATCH" (اللي كتبان ملي `callScreenActive` مفعّل) كانت `return`
  مبكرة قبل ما يوصل الكود لمتغير `callAnimationOverlay`، فكانت أنيميشن
  الاستدعاء (اللي كيشغلها زر "استدعاء لاعب") ما عندهاش فين تتعرض حتى تولي
  الشاشة العادية (بالضغط على "إلغاء الاستدعاء"). دابا `callAnimationOverlay`
  كيتحسب قبل هاد الشاشة وكيتعرض فيها مباشرة.
- **شاشة "NEXT ON THE MATCH" (قبل المباراة) فمود بار إيكيب** دابا كتبين: صورة
  الفريق + شعار النادي (زوج صور صغار فوق اسم الفريق)، واسماء **جميع لاعبي
  الفريق** تحت (ماشي غير اللاعب الحالي). اسم النادي كان يبان ديجا (badge تحت
  الاسم).
- **شاشة إعلان الفائز فالأوبيراتور** (`OperatorScreen.tsx`، الشاشة اللي فيها
  الكأس 🏆 و"Return to Tournament"/"PDF") دابا فيها نفس التحسينات اللي درنا
  فشاشة الجمهور: صورة الفريق + شعار النادي + اسم النادي + أسماء جميع اللاعبين.
- **أعلام الجنسية اللي ما كانوش بانين** — كانت الصورة الشخصية (لي تفضّلتي
  تحطيها) كتخبي العلم بالكامل (كان مصمم هكذا: صورة أو علم، ماشي بجوج). دابا:
  زدنا علم صغير فزاوية الصورة، وعلم آخر جنب رمز الجنسية (CHN/TUR) فأعلى
  الشاشة الرئيسية ديال المباراة — كيبانوا الجوج مع بعضياتهم.

## -1. تحكم فيما يظهر فشاشة الاستدعاء (Par Équipe)
- زدنا لوحة جديدة فـ Admin Panel اسمها **"شاشة الاستدعاء — إظهار/إخفاء"** (جنب لوحة
  "Public Display" الموجودة ديجا)، فيها 11 مفتاح إظهار/إخفاء مستقلين لكل عنصر
  فأنيميشن الاستدعاء: صورة الفريق، شعار النادي، علم/دولة الفريق، لائحة أسماء
  اللاعبين، صور/أعلام اللاعبين الإضافيين، اسم البطولة، صورة اللاعب المستدعى،
  علم اللاعب المستدعى، أرقام اللاعب، الفئة/الوزن، والجنس.
- التغيير كيتطبق **فوريا** فشاشة الجمهور (بحال لوحة Public Display)، حتى فوسط
  المباراة، عن طريق `SET_CALL_DISPLAY_CONFIG` جديد فـ `MatchContext.tsx`
  (كيتزامن تلقائيا لأنو الحالة كاملة كتتبعث عبر الـ realtime broadcast).
- النوع الجديد `CallDisplayConfig` مزاد فـ `src/types/tkd.ts`، والمفتاح القديم
  "Show player photos in Team Call animation" (فإعدادات Par Équipe) بقا خدام
  ومزامن مع المفتاح الجديد `showRosterPhotos` باش مايختلفوش.
- الملفات: `src/types/tkd.ts`، `src/context/MatchContext.tsx`،
  `src/components/AdminPanel.tsx`، `src/components/PublicScoreboard.tsx`.

## 0. تتمة بار إيكيب: منطقة مستقلة للصور، شاشة الفائز الكاملة، وحفظ الروستر
- **منطقة مستقلة لصور/أعلام اللاعبين الإضافيين** فوق لائحة الأسماء فمرحلة نداء
  الفريق (بدل ما كانت الصورة مدمجة فكل سطر) — شبكة صور مربعة بعلم صغير فكل واحدة،
  مرتبة تصاعديا برقم اللاعب، تبان غير كي `showRosterPhotosInTeamCall` مفعّل.
  الملف: `src/components/PublicScoreboard.tsx`.
- **شاشة إعلان الفائز (Par Équipe)** دابا كتبين: إطار صورة الفريق + إطار شعار
  النادي (بحال فمرحلة النداء)، اسم النادي جنب طريقة الفوز، و**أسماء جميع لاعبي
  الفريق الفائز** (ماشي اسم واحد) مرتبين برقم اللاعب. الملف: نفسو.
- **حفظ مباراة بار إيكيب بكل تفاصيلها**: عند انتهاء المباراة كان كيتسجل
  `team_names`/`team_logos` غير، ودابا كيتسجل معاهم `club_logos` و`team_country`
  و**`team_roster`** (كل اللاعبين: الاسم، الجنسية، الرقم) — الأعمدة كانت موجودين
  فقاعدة البيانات (migrations `20260805000000` و`20260805010000`) ولكن ماكانوش
  كيتسجلو عند الحفظ الفعلي. الملفات: `src/components/OperatorScreen.tsx`،
  `src/lib/match-local.ts`.
- **شاشة البحث عن المباريات (ResultView)** كتبين دابا روستر الفريقين كاملين
  (كل الأسماء) للمباريات المحفوظة من نوع بار إيكيب، بفضل `team_roster` المحفوظ.
  الملف: `src/components/ResultView.tsx`.
- ملاحظة: زر "استدعاء اللاعبين" / "إلغاء الاستدعاء" وأنيميشن استبدال نداء الفريق
  بنداء اللاعب كانو موجودين ديجا (`SET_CALL_SCREEN`, `START_CALL_ANIMATION`) —
  تأكدنا أن السلوك سليم بلا حاجة لتعديل.

## 1. لائحة اللاعبين في شاشة نداء الفريق (بار إيكيب)
- عند استدعاء الفريق (المرحلة الأولى: شعار + اسم الفريق)، تظهر الآن تحت الاسم لائحة
  كامل لاعبي الفريق: **الاسم + الجنسية + الرقم**، مرتبين **تصاعديا** حسب رقم اللاعب
  (وإلا رقم البذرة إذا ماكانش رقم اللاعب).
- **بدون صور** في هاذ المرحلة — الصور تبان غير فمرحلة نداء اللاعب الفردي بعدها.
- الملفات: `src/types/tkd.ts` (`CallAnimation.roster`)، `src/context/MatchContext.tsx`
  (`buildTeamRosterList`)، `src/components/PublicScoreboard.tsx` (العرض).

## 2. جنس اللاعب في شاشة نداء اللاعب
- زدنا `gender` لـ `CallAnimation`، مربوطة بجنس فئة المباراة (اللي كاين أصلا فالنظام).
- تبان الآن جنب الرقم والتصنيف فمرحلة نداء اللاعب الفردي.

## 3. إصلاح موثوقية اتصال الويفي (حاسوب ↔ هاتف)
- **السبب الأول:** سيرفر الـ WebSocket (`electron/main.cjs`) ماكانش عندو أي معالجة للأخطاء.
  إلا بقا البورت 8787 محجوز (مثلا التطبيق تسد وحل بسرعة)، السيرفر كايفشل بصمت
  ومايبقاش يعاود المحاولة.
- **السبب الثاني (فبرنامج الحاسوب/الأوبيراتور، بحال ما تفضلتي وأشرتي):** الـ IP
  ديال الحاسوب كان كيتقرا مرة وحدة فقط (عند تشغيل السيرفر أو عند الضغط اليدوي على
  "Re-check"). إلا الحاسوب بدل شبكة، ولا جدد الـ DHCP lease، ولا شعل/طفا الهوتسبوت
  ديالو وسط البطولة، العنوان اللي كيبان للحكام يبقى قديم بصمت (السيرفر ماكايتوقفش
  ولا كايعطي خطأ) — وهادشي بالضبط اللي كايبان كـ"احيانا ينجح احيانا لا".
- **الإصلاح:**
  - إعادة محاولة تلقائية للسيرفر (retry + backoff تصاعدي: 1s → 2s → 4s → 8s → 15s)
  - زر **"Restart connection"** يبان فشاشة الأوبيراتور غير كاين مشكل
  - مؤشر حالة حي: Starting… / Server ready / Server error
  - **مراقبة تلقائية للـ IP** (فبرنامج الحاسوب) كل 4 ثواني — إلا تبدل العنوان،
    شاشة الأوبيراتور وكود QR كيتحدثو وحدهم بلا ما يحتاج المستخدم يضغط "Re-check"
- الملفات: `electron/main.cjs` (`startIpWatcher`)، `electron/preload.cjs`،
  `src/lib/localWifiCentral.ts`، `src/components/OperatorScreen.tsx`.

## 4. تفعيل/إلغاء القبول المباشر لنقاط الحكام الجانبيين
الزر/الإعداد موجود دابا فـ **3 أماكن** (مش غير فشاشة الأوبيراتور الحية):

1. **شاشة الأوبيراتور (حي، وسط المباراة)** — `OperatorScreen.tsx`: زوج أزرار
   "Require Approval" / "Auto-Approve" جنب اختيار وضع اتصال الحكام. تبديل فوري
   بلا ماتوقفي المباراة.
2. **Admin Panel (إعدادات المباراة الفردية قبل البداية)** — `AdminPanel.tsx`:
   checkbox "Auto-Approve Side Judge Scores" مع شرح، جنب Golden Point Round.
3. **إعدادات البطولة (Knockout / League / Friendly)** — `TournamentManager.tsx`:
   checkbox "Auto-Approve Judge Scores" فقسم القواعد (Rules)، كيتطبق على كل
   مباريات البطولة.
4. **إعدادات بار إيكيب (Par Équipe)** — `pages/Index.tsx`: checkbox "قبول تلقائي
   لنقاط الحكام الجانبيين" فقسم قواعد الفريق، كيتطبق على مباراة الفريق.

فكل الحالات: الإعداد الافتراضي هو **إلغاء التفعيل** (كيفما كان دائما — يعني
النقطة كتحتاج موافقة)، وتقدر تفعليه فأي واحدة من هاذ الشاشات، أو تبدليه حي من
شاشة الأوبيراتور فأي وقت.

- الملفات: `src/types/tkd.ts` (`MatchConfig.autoApproveJudgeScores`)،
  `src/context/MatchContext.tsx` (action جديد `UPDATE_CONFIG`)،
  `src/components/OperatorScreen.tsx`، `src/components/AdminPanel.tsx`،
  `src/components/TournamentManager.tsx`، `src/pages/Index.tsx`.

---

**تم التحقق:** `npm install` + `npx tsc --noEmit` + `npm run build` نجحو، وما
زدناش أي خطأ TypeScript جديد (تقارنت مع النسخة الأصلية — نفس الأخطاء القديمة
الموجودة من قبل باقية كيفما هي، فالأغلب بسبب ملف أنواع Supabase اللي ماتجدد
بعد آخر migrations).

## 5. تشخيص وإصلاح: تكرار معالجة نقطة الحكم الجانبي + عطل "Require Approval"

**السبب الجذري (نفسه للمشكلتين معا):** الـ `useEffect` المسؤول عن الاشتراك فقناة
`judge-votes` (فـ `OperatorScreen.tsx`) كان معتمد فمصفوفة الـ dependencies على
`connectedJudgeCount` و`wifiJudges` — وهاذوا الاثنين كيتبدلو **من داخل نفس الـ
effect** (عبر presence sync handler). هاذشي كيخلق حلقة: subscribe ← presence
sync كيبدل العدد ← الـ dependency تبدلت ← React كيدير cleanup (unsubscribe) ثم
يعاود subscribe من جديد ← presence sync من جديد... وهكذا مع كل انضمام/مغادرة
حكم، وحتى مع كل تبديل لـ `state.status` أو لإعداد Auto-Approve.

هاذ الدورة عندها نتيجتين خطيرتين:
1. **إعادة الاشتراك المتكررة كتخلق قنوات "زومبي"**: `supabase.channel(name)`
   كيخلق كائن جديد فكل مرة (ماشي singleton)، و`removeChannel()` غير متزامن
   (async). إلا وقع resubscribe بسرعة، القناة القديمة تقدر تبقى "حية" ومتصلة
   بنفس الـ topic لحظة قبل ما تنمسح فعليا، فبرودكاست وحد كيوصل لعدة نسخ من
   الـ handler فنفس الوقت. هاذشي كيفسر بالضبط "ضغطة وحدة زادت 35 نقطة" — 35
   قناة "زومبي" تراكمت من الجولة الأولى للمباراة.
2. **كل قناة "زومبي" حابسة قيمة قديمة (stale closure) لـ
   `state.config.autoApproveJudgeScores`**: إلا كان Auto-Approve مفعل شي مرة
   وبعدين تبدل لـ Require Approval، القنوات القديمة اللي بقاو عالقين مازالو
   شايفين `autoApproveJudgeScores === true` (الفريزة ديالهم القديمة) وكيكملو
   يزيدو النقطة مباشرة بلا انتظار — وهاذشي بالضبط اللي بان كـ"Require Approval
   ما خدمش".

**الإصلاح المطبق (`src/components/OperatorScreen.tsx`):**
- الـ effect دابا معتمد غير على `[connMode]` (لازم يتبدل غير مع تبديل النقل
  Internet/Wi-Fi، ماشي مع أي حاجة أخرى)، وكل الـ handlers كيقراو القيم الحية
  (status، ptgActive، autoApprove، activeJudgeCount، refereeName) من `liveRef`
  (ref كيتحدث فـ effect خفيف منفصل بلا ما يعاود subscribe القناة).
- زدنا **حارس idempotency** (`markVoteProcessed(vote.id)`): أي `vote.id` تعالج
  مرة وحدة وتزاد النقطة، أي محاولة ثانية لنفس الـ id (من قناة زومبي، إعادة
  إرسال، أو حتى تصويت الحكم الرئيسي وتصويت حكم جانبي كيوصلو فنفس اللحظة) —
  كتتجاهل. هاذ الحارس مطبق فـ 3 أماكن: نداء الفريق المباشر (auto-approve)،
  وصول التصويت (score-vote-update لما توصل الأغلبية)، وزر الحكم الرئيسي
  (castRefereeVote) — الثلاثة كيقدرو يزيدو نفس النقطة، فخصهم كلهم يحترمو نفس
  الحارس.

**تحقق:** `tsc --noEmit` و`npm run build` نجحو بلا أي خطأ جديد.

## 6. تقوية بنية حفظ البيانات (Supabase) + نسخ احتياطي كامل

### أ) اكتشاف: التطبيق كان كيكتب فSupabase بلا "تسجيل دخول"
كودك يدير `insert`/`update`/`delete` مباشرة على `matches`, `players`, `tournaments`,
`clubs` بلا ما يستدعي أي `supabase.auth.signIn...`. RLS policy موجودة من قبل
(`20260702163137_...`) كتقيد الكتابة بـ role `authenticated` فقط. **الإصلاح**
(`src/integrations/supabase/client.ts`): زدنا `signInAnonymously()` تلقائي عند
تشغيل التطبيق — كل جلسة (متصفح/Electron) كتاخذ هوية `authenticated` وحدها بلا
شاشة تسجيل دخول، باش الحفظ يخدم بثقة حسب القواعد المكتوبة أصلا.

⚠️ **خطوة مطلوبة منك فـ Supabase Dashboard**: Authentication → Sign In / Providers
→ فعلي **"Anonymous Sign-Ins"**. بلا هاذشي، الحفظ يبقى يفشل.

### ب) migration جديدة: `20260804000000_strengthen_schema_indexes_and_updated_at.sql`
- فهرسة (indexes) على كل الأعمدة المرجعية (foreign keys) والمفلترة بكثرة
  (tournament_id، chung/hong_player_id، match_id، status، mat_number...) —
  كتسرع الاستعلامات مع تراكم تاريخ المباريات عبر مواسم.
- عمود `updated_at` + trigger تلقائي على `matches`, `tournaments`, `players`,
  `clubs` — تتبع آخر تحديث لكل صف (مفيد للتشخيص ولأي أداة مزامنة/نسخ مستقبلية).

### ج) نسخ احتياطي كامل من داخل التطبيق (`src/lib/backup.ts`)
زر جديد فـ Tournament Manager: **"نسخة احتياطية كاملة (سحابة)"** — كيصدر **كل**
البيانات من Supabase (بطولات، مباريات، لاعبين، أندية، سجل الأحداث، القوالب) —
من جميع الأجهزة، ماشي غير المتصفح الحالي — كملف JSON واحد قابل للتنزيل. وزر
**"استرجاع (سحابة)"** كيرجع البيانات من هاذ الملف (upsert بالـ id، بلا ما يمسح
شي حاجة أخرى).

هاذشي **مكمّل** للنسخ الاحتياطي التلقائي ديال Supabase نفسها (PITR/daily backups
— محتاج خطة Pro مدفوعة، وماشي قابل للتنزيل من الداشبورد أصلا) — يعني عندك دابا
شبكة أمان مجانية وقابلة للحمل، بلا ما تحتاجي ترقية الخطة.

**تحقق:** `tsc --noEmit` و`npm run build` نجحو بلا أي خطأ جديد.

## 7. السبب الحقيقي المتبقي لتكرار نقطة الحكم الجانبي (x4): لا قفل وحيد للتطبيق

### التتبع الكامل (JudgePanel → Supabase → OperatorScreen → reducer)
تتبعت السلسلة كاملة من زر الحكم الجانبي (`JudgePanel.tsx`) حتى الـ reducer:
- `handleSendScore()` كيبعث `score-vote-request` **مرة وحدة بالضبط** لكل ضغطة (تأكدت،
  ماكاينش تكرار من جهة الحكم).
- ماكاينش listener خفي آخر كيدير `dispatch(ADD_SCORE)` — غير 3 أماكن، وكلهم فيهم
  حارس `markVoteProcessed`.
- القناة `judge-votes` ماشي مكررة جوة نفس نافذة الأوبيراتور (الإصلاح السابق ثابت).

**السبب الحقيقي:** `electron/main.cjs` ماكانش فيه أي حماية ضد تشغيل التطبيق أكثر
من مرة وحدة فنفس الوقت (`app.requestSingleInstanceLock()` ماكانتش موجودة). كل
ضغطة زايدة على أيقونة التطبيق (بالغلط، أو نسخة قديمة بقات مفتوحة فالخلفية)
كتخلق **process كامل جديد ومستقل** — بنافذة Operator جديدة، وباشتراك جديد
ومستقل فقناة `judge-votes` بنفس المفتاح. ضغطة وحدة من الحكم الجانبي كتوصل
لكل نسخة مفتوحة **فنفس الوقت**، وكل وحدة كتزيد النقطة على الحالة المحلية
ديالها وتبعثها عبر `match-sync` — يعني "x4" كانت كتعكس بالضبط عدد النسخ
المفتوحة فنفس اللحظة، ماشي خطأ فمنطق التصويت (اللي كان صحيح فعلا).

### الإصلاح (`electron/main.cjs`)
زدنا `app.requestSingleInstanceLock()` فبداية التطبيق: أي محاولة تشغيل تانية
كتسد نفسها فورا وكتركز (focus) النافذة الأصلية بدل ما تفتح نسخة جديدة موازية.

**تحقق:** `node --check electron/main.cjs`، `tsc --noEmit`، و`npm run build`
نجحو بلا أي خطأ.

## 8. لون كلمة WINNER كان كيندمج مع الإطار الذهبي

الإطار/خلفية بانر "WINNER" ذهبية (`--gradient-gold-metal`)، ولون الكلمة قابل
للتخصيص (`sb.winnerColor`) وافتراضيا ذهبي بحال `#f5c842` — يعني الكلمة كانت
كتندمج مع الإطار فبعض الألوان. زدنا **حد أسود غامق ثابت** حول الحروف
(`-webkit-text-stroke`) بلا ما نمسو اللون القابل للتخصيص، باش الكلمة تبقى
واضحة دائما بغض النظر شنو اللون اللي يختارو المستخدم للنص أو للإطار.
الملف: `src/components/PublicScoreboard.tsx` (`.sb-winner` CSS).

## 9. لائحة دول قابلة للبحث + علم فحقل الجنسية (لاعبين)

حقل "الجنسية" لكل لاعب (فـ Par Équipe وفالفرق الفردية) كان نص حر بلا مساعدة —
دابا فيه بحث + علم لكل دولة، بنفس اللائحة المستعملة فـ Admin (`TKD_COUNTRIES`).
مكون جديد قابل لإعادة الاستعمال: `src/components/CountryPicker.tsx`، مربوط فـ:
- `src/pages/Index.tsx` (نموذج إضافة لاعب فبار إيكيب)
- `src/components/TournamentManager.tsx` (نموذج إضافة لاعب فالروستر)

(حقل جنسية اللاعب الفردي الرئيسي كان عندو ديجا بحث مشابه — تركناه كيفما هو).

**تحقق:** `tsc --noEmit` و`npm run build` نجحو بلا أي خطأ جديد.

## 10. تشخيص وإصلاح: "الوقت كيتوقف بلا سبب" + "النقطة التانية ما توصلش" (JudgePanel)

### التشخيص
تأكدت أولا أن التوقف **ماشي PTG** (لا راية ذهبية تبان — تحقق مع المستخدم). البحث
الشامل فكل نقطة كيتصور فيها `status: 'paused'` فالكود ما لقاش أي مسار غير شرعي
مربوط بنقطة الحكم الجانبي فـ `OperatorScreen.tsx`/`match-engine.ts`.

**السبب الحقيقي كان فجهة الحكم (`JudgePanel.tsx`)، ماشي فجهة الأوبيراتور:**
شاشة الحكم الجانبي عندها **ساعة محلية خاصة بيها** (نفس `MatchProvider` اللي
كيلف التطبيق كامل)، كتتحدث فقط عبر بث `match-sync` جاي من الأوبيراتور. قناتي
Supabase (`judge-votes` و`match-sync`) فـ `JudgePanel.tsx` **ماكانش عندهم أي
منطق لإعادة الاتصال** إلا انقطعو — وهاذشي شائع بزاف فالهواتف: المتصفح كيوقف/
يضعف الاتصالات (WebSocket) لما تقفل شاشة الهاتف أو يدخل التطبيق للخلفية (وهو
بالضبط نمط استعمال حكم جانبي واقف بهاتفو). بمجرد ما تنقطع القناة:
- شاشة الحكم كتبقى عالقة على آخر حالة توصلت (الوقت يبان واقف، بلا ما يتحرك)
- أي نقطة كيصفطها الحكم بعدها **ما توصلش للأوبيراتور** لأن القناة ميتة
- بلا أي إشعار بصري للحكم إنو منقطع — كيبان التطبيق "خدام" ولكن الحقيقة لا

### الإصلاح (`src/components/JudgePanel.tsx`)
- معالجة حالات `CHANNEL_ERROR` / `TIMED_OUT` / `CLOSED` عند الاشتراك، وليس
  فقط `SUBSCRIBED`
- إعادة اتصال تلقائية فورية عند: رجوع الصفحة للواجهة (`visibilitychange` —
  أهم حالة، كتغطي قفل/فتح شاشة الهاتف)، أو رجوع الانترنت (`online` event)
- **مؤشر اتصال مرئي** يبان للحكم مباشرة "انقطع الاتصال — جاري إعادة المحاولة"
  مع زر "أعد المحاولة الآن" للتدخل اليدوي الفوري

### تأكيد: زر Auto-Approve / Require Approval
راجعت المنطق من جديد (`OperatorScreen.tsx`): الفرع صحيح — `Require Approval`
كيوقف النقطة فـ `judgeNotifications` (معلقة) حتى توصل الأغلبية الموزونة، ما
كتضافش مباشرة. `Auto-Approve` كتضاف فورا. الإصلاحات السابقة (تثبيت الـ effect
على `[connMode]` فقط + `app.requestSingleInstanceLock()`) كانت السبب فأي
سلوك غريب سابق — الزر نفسو خدام صحيح.

**تحقق:** `tsc --noEmit` و`npm run build` نجحو بلا أي خطأ جديد.

## 11. السبب الدقيق والمحدد لانقطاع الاتصال بعد كل نقطة (JudgePanel)

بعد توضيح إضافي منك ("الاتصال كيقطع بعد أي نقطة، بشكل ثابت — ماشي عشوائي")،
لقيت سبب أدق وأخطر من فرضية "قفل الشاشة" وحدها:

**كل زر فشاشة الحكم (إرسال نقطة، تصويت، تنبيه، طلب توقف) كان كيدير
`supabase.channel('judge-votes')` من جديد فكل ضغطة** — بدل ما يستعمل القناة
**المتصلة أصلا** اللي تخلقات مرة وحدة عند فتح الصفحة. كل ضغطة كانت كتخلق كائن
قناة "يتيم" جديد (بنفس الاسم `judge-votes`) بلا `.subscribe()` وبلا تنظيف —
وهاذشي كيهز استقرار القناة الحقيقية المتصلة، وهو السبب فانقطاع الاتصال بعد كل
نقطة (بشكل ثابت ومتكرر، ماشي عشوائي).

**الإصلاح (`src/components/JudgePanel.tsx`):** زدنا `channelRef` كيحتفظ
بالقناة المتصلة الوحيدة، ودالة مشتركة `sendJudgeEvent()` كيستعملها **كل**
الأزرار الأربعة بدل ما يخلق كل واحد قناته الخاصة. إلا كانت القناة مقطوعة وقت
الضغط، كتبدأ إعادة اتصال فورية بدل ما الإرسال يضيع بصمت.

**تحقق:** `tsc --noEmit` و`npm run build` نجحو بلا أي خطأ جديد.

## 12. Admin: إعدادات "بار إيكيب" مباشرة عند اختيارها (جزء من #3+#4)

كي تختاري "Competition Type: par_equipe" فـ Admin، يبان تلقائيا قسم جديد
**"Par Équipe — Team Settings"** فيه كل شيء بلا ما تخرجي من Admin:

- **وضع إدارة اللاعبين**: راوندات ثابتة لكل لاعب (rotation) أو تبديل ديناميكي
  وسط الراوند (substitution)
- **زر "إظهار صور اللاعبين فأنيميشن استدعاء الفريق"** (persisted فـ
  `MatchConfig.showRosterPhotosInTeamCall`) — الافتراضي: بلا صور (كيفما كان)
- **لكل فريق (Chung/Hong)**: اسم الفريق/النادي، الدولة (بحث + علم)، شعار
  النادي (رفع ملف أو رابط)، ولائحة كاملة للاعبين (اسم، جنسية، رقم، صورة) مع
  إضافة/حذف

**زر "Save" فـ Admin دابا كيحفظ كل هاذشي**: كيبعث للحالة المركزية (نفس
الأكشن اللي كتستعملو شاشة بار إيكيب المستقلة)، ويخزنو فقاعدة البيانات
(`matches.team_names`, `team_logos`, `team_country`, `team_roster` — جدولين
جدد زدناهم `team_country`/`team_roster` عبر migration جديدة). يعني المباراة
تقدر تترجع بالكامل (فريق، لاعبين، صور) من قاعدة البيانات بلا ما تعاود دخول
المعلومات.

## 13. أنيميشن استدعاء الفريق: عرض الدولة (علم + اسم)

زدنا `teamCountry` لحالة المباراة ولـ `CallAnimation` — كيبان دابا العلم واسم
الدولة تحت اسم الفريق مباشرة فمرحلة نداء الفريق (بين شعار الفريق واسمه، ولائحة
اللاعبين). مربوط بحقل "Country" الجديد فـ Admin (نفس الفكرة تنطبق لما يتزاد
لاحقا فـ Tournament Manager / بار إيكيب المستقلة).

## 14. صور اللاعبين فأنيميشن استدعاء الفريق — قابلة للتفعيل

لائحة اللاعبين اللي تبان فنداء الفريق (من التحديث السابق) دابا تقدر تعرض صور
اللاعبين أيضا (دائرية، بحدود بلون الفريق، مع fallback بحروف أول الاسم إلا
مكانتش صورة) — ولكن غير إلا كان الإعداد الجديد مفعّل. الافتراضي يبقى بلا صور،
كيفما تحدد من قبل، والصور تبان غير عند نداء كل لاعب فرديا.

---

**ملاحظة مهمة:** هاذشي كيغطي جزء كبير من #3 (Admin) وجزء من #4 (الحفظ الشامل
— على الأقل من زر Save فـ Admin) و#1 (جزء الدولة والصور فأنيميشن الاستدعاء).
باقي:
- تصميم بصري أكثر تميزا لشعار الفريق فالأنيميشن (تأثير دخول خاص) — التحسين
  الوظيفي (الدولة، الصور) موجود، التحسين الجمالي (تأثيرات حركية إضافية) لا
- أنيميشن تبديل اللاعب (#2) بنفس خلفية أنيميشن الاستدعاء، معلومات اللاعب
  فوسط الشاشة — لم يُبنى بعد
- تدقيق شامل لزر الحفظ فشاشة بار إيكيب المستقلة (`pages/Index.tsx`) — الأوبيراتور
  محدث، الشاشة المستقلة لسا محتاجة نفس المراجعة

## 15. صورة الفريق منفصلة عن شعار النادي (تأكيد + إكمال فكل الشاشات)

راجعت الكود ولقيت أن التفرقة بين **صورة/شعار الفريق** (بارزة، كبيرة، فوسط
الشاشة، تأثير دخول خاص) و**شعار النادي** (شارة صغيرة ثانوية فزاوية الشعار
الكبير) **موجودة بالفعل** فـ:
- `types/tkd.ts` (`teamLogos` = شعار الفريق، `clubLogos` = شعار النادي، منفصلين)
- `AdminPanel.tsx` (حقلين منفصلين: "Team Logo" و"Club Logo")
- `PublicScoreboard.tsx` (الفريق بارز فالوسط بحجم كبير + تأثير `teamLogoReveal`،
  والنادي شارة صغيرة فالزاوية)
- Migration مخصصة: `20260805010000_add_club_logos.sql`

**اللي كان ناقص وكملتو دابا:** شاشة **بار إيكيب المستقلة** (`pages/Index.tsx`)
عندها أصلا حقول `clubPhoto` و`country` لكل فريق فالواجهة، ولكن الحفظ النهائي
(`dispatch SET_MATCH_INFO`) كان كيبعث غير `teamLogos` بلا `clubLogos` ولا
`teamCountry` — يعني الصورة/الدولة تتكتب فالواجهة ولكن ماكانتش توصل للمباراة
الفعلية. تصلح: دابا كيبعث الثلاثة معا.

⚠️ **باقي:** `TournamentManager.tsx` (البطولات بنظام الكأس/الملقى) عندها
`teamLogo` واحد بلا تفرقة نادي/فريق ولا دولة منفصلة — نفس المراجعة لسا
محتاجة تنبنى هناك.

## 16. حفظ تلقائي لمسودة إعداد بار إيكيب (السلامة — جزء من #4)

**المشكل:** شاشة بار إيكيب المستقلة (`pages/Index.tsx`) كانت ماتحفظ حتى حاجة
قبل الضغط على "ابدأ المباراة" — كل شيء (لاعبين، صور، أسماء فرق) كان يعيش غير
فالذاكرة المؤقتة. أي كراش أو تحديث صفحة بالغلط كان كيضيع كل شيء بلا رجعة.

**الإصلاح:** كل تغيير (لاعبين، فرق، قواعد، اسم المنافسة) كيتحفظ أوتوماتيكيا
(debounced، 300ms) فـ `localStorage` تحت مفتاح `tkd-parequipe-draft`. عند فتح
شاشة بار إيكيب من جديد، كل شيء كيرجع بحاله — بلا ما تعاود دخول شي حاجة. زدنا
زر **"Start Fresh"** للي بغا يبدا من الصفر عمدا (كيسولك تأكيد قبل ما يمسح).

**تحقق:** `tsc --noEmit` و`npm run build` نجحو بلا أي خطأ جديد.

---

### الوضع العام دابا (الطلب الكبير ديال بار إيكيب):
✅ #3 (Admin: إعدادات بار إيكيب) — منجز بالكامل
✅ #4 (الحفظ الشامل): Admin منجز، بار إيكيب المستقلة منجز (auto-save draft +
   إصلاح clubLogos/teamCountry)، **TournamentManager (البطولات) لسا محتاج مراجعة**
✅ #1 (جزء): الدولة، صور اللاعبين، صورة الفريق/النادي منفصلين، تأثير دخول
   بصري للشعار — كلهم منجزين
❌ #2: أنيميشن تبديل اللاعب (نفس خلفية الاستدعاء، معلومات فالوسط) — لم يُبنى بعد

## 17. راجعت البرومبت الكامل من جديد ولقيت 3 فجوات حقيقية، صلحتهم

### أ) "تسجيل النقاط بين الراوندات" — كان موجود فالكود ولكن غير قابل للتبديل
حقل `scoreResetPerRound` كان موجود من قبل (كيتحكم فـ: النقط/الإنذارات تتراكم
عبر الراوندات ولا ترجع 0 فكل راوند)، ولكن كان **مثبت (hardcoded)** على `false`
فـ 3 أماكن (Index.tsx، TournamentManager.tsx) بلا ما المستخدم يقدر يبدلو. زدت
checkbox **"Carry score across rounds"** فـ 3 الشاشات (Admin، بار إيكيب
المستقلة، TournamentManager) كلهم مربوطين بنفس الحقل.

### ب) رسالة خطأ عند فشل الحفظ — كانت غايبة تماما
`handleSave` فـ Admin كان عند فشل الحفظ (مشكل شبكة، RLS...) **يكتب الخطأ فـ
console فقط** — المستخدم مايعرفش والو، كيبان الزر عادي وكيظن أن الحفظ نجح.
دابا كاين رسالة حمراء واضحة تحت الزر: "⚠ Save failed: [تفاصيل الخطأ]" وكتبقى
بانة حتى ينجح حفظ جديد.

### ج) تأكيد: زر "Saved!" البصري موجود من قبل وخدام
الزر كيبدل لـ "✓ Saved!" لمدة ثانيتين بعد نجاح الحفظ — كان موجود أصلا وخدام
مزيان، غير ماكانش عندو نظير فحالة الفشل (تصلح فـ ب).

**تحقق:** `tsc --noEmit` و`npm run build` نجحو بلا أي خطأ جديد.

---

### ملخص الفجوات المتبقية (بعد المراجعة الكاملة للبرومبت):
1. **#2 (أنيميشن التبديل)** — لسا ماتبناش، الأكبر المتبقي
2. **إطارات فردية لكل لاعب** فلائحة الاستدعاء (حاليا صفوف فقائمة واحدة، ماشي
   "إطار" منفصل بستايل موحد لكل لاعب) — تحسين بصري، وظيفيا خدام
3. **TournamentManager**: شعار وحد بلا تفرقة نادي/فريق ولا دولة (بخلاف Admin
   وبار إيكيب المستقلة اللي عندهم التفرقة كاملة)

## 18. أنيميشن تبديل اللاعب (#2) — كانت موجودة ديجا، زدت اللمسة الأخيرة

اكتشفت أن `CONFIRM_SUBSTITUTION` (فـ `MatchContext.tsx`) كانت أصلا كتستدعي
**نفس الكومبوننت** ديال نداء اللاعب الفردي (`buildPlayerCallPhase` → نفس
`phase: 'player'` اللي كيرندر بيه `PublicScoreboard.tsx`) — يعني التصميم
المطلوب فـ #2 (نفس الخلفية، معلومات وصورة اللاعب فالوسط، كومبوننت واحد
لإعادة الاستعمال بـ mode مختلف) **كان منفذ من الأصل** عبر `phase: 'team' |
'player'` بدل تسميات `mode="team_call"`/`mode="player_substitution"` الحرفية
— نفس الفكرة بالضبط.

**اللي كان ناقص وزدتو:** ما كانش كاين أي علامة تفرق للمشاهد "هاذي تبديل
لاعب وسط المباراة" عن "هاذي أول دخول للاعب". زدت:
- `CallAnimation.isSubstitution` (flag جديد)
- شارة **"Player Change / تبديل اللاعب"** تبان فوق البطاقة كي يكون تبديل
  (`CONFIRM_SUBSTITUTION` كيمررها `true`)، وما تبانش عند النداء العادي

**التوقيت:** تأكدت أن `REQUEST_SUBSTITUTION` كتوقف الوقت (PAUSE) قبل ما تبدا
هاد الأنيميشن، ولا `CONFIRM_SUBSTITUTION` كتلمس الـ status — يعني الوقت
كيبقى واقف بالضبط كيفما كان محدد، والأوبيراتور هو اللي كيرجعو يدويا.

**تحقق:** `tsc --noEmit` و`npm run build` نجحو بلا أي خطأ جديد.

---

**الوضع النهائي دابا للطلب الكبير (4 نقط):** #1 ✅ #2 ✅ #3 ✅ #4 ✅ (الأربعة
منجزين). الباقي غير تحسينات بصرية اختيارية (إطارات فردية للاعبين، توحيد
شعار الفريق/النادي فـ TournamentManager) مو مطلوبة صراحة.


## المرحلة الجديدة — Multi-Mat Assignment + Weight Workload + Central Wall
- إضافة رقم بساط ثابت لكل كمبيوتر من Admin → Multi-Mat.
- إضافة اسم الجهاز (مثال MAT-02-PC).
- Operator يقرأ رقم البساط تلقائيًا، ولا يسمح بتغييره يدويًا أثناء وجود تعيين للجهاز.
- كل الأجهزة تتصل بنفس Supabase؛ كل جهاز يرسل heartbeat تحت رقم بساطه.
- Control Room يعرض رقم البساط واسم الكمبيوتر والحالة والمباراة واللاعبين.
- إضافة `/tournament-wall` كشاشة مركزية قراءة فقط تعرض جميع البسطات والمباراة الحالية والمباراة التالية.
- إضافة خطة MAT WORKLOAD لكل بطولة: يمكن تخصيص عدة أوزان لكل بساط.
- AUTO QUEUE يحترم الأوزان المخصصة.
- منع تعارض اللاعب: لا يتم وضع لاعب حي أو لاعب موجود في Queue على بساط آخر.
- الحفاظ على V35 وIndividual وPar Équipe وPublic Display دون تغيير في مصدر الحقيقة للنتائج.
