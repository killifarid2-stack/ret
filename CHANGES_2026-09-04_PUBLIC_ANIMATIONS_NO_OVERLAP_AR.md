# إصلاح شاشة العرض الثانية والـMini Preview — 2026-09-04

- إصلاح Team Call داخل Live Preview: لم يعد يستخدم أبعاد شاشة المشغل `100vw/100vh` داخل إطار المعاينة المصغرة، وأصبح محصورًا داخل Canvas المعاينة 1920×1080.
- منع Team Call من إضافة `team-call-fullscreen-active` إلى `html/body` عند تشغيله داخل المعاينة المصغرة، حتى لا يؤثر على واجهة الحكم الرئيسية.
- شاشة العرض الثانية الحقيقية تبقى Fullscreen مستقلة، بينما Mini Preview يبقى داخل إطاره فقط.
- الحفاظ على فصل Team Logo في أعلى البطاقة وClub Logo في الشريط السفلي.
- الحفاظ على كأس WAB-TKD الأصلي الموجود في `src/assets/trophy-wab-tkd-transparent.png` كعنصر واحد في الوسط.
- `FlagImage` أصبح يبحث أولًا في `src/assets/flags/*.svg` عبر `getLocalFlagUrl` بدل الاعتماد على صور خارجية؛ وعند عدم توفر العلم محليًا يستخدم fallback علم Unicode بدل صورة مكسورة.
- Player Call الخاص بالمباريات الفردية أصبح يستخدم الأعلام المحلية أيضًا بدل FlagCDN الخارجي.
- لم يتم تغيير منطق النتائج أو النقاط أو توقيت Winner Animation في هذه الدفعة؛ ملف Winner Animation المرجعي الذي سيُرسل لاحقًا يمكن دمجه وتوقيته على نفس بنية العرض بدون إعادة بناء النظام من الصفر.
